<!-- -*- mode: markdown; -*- -->

Предстоит посмотреть и прокомментировать код, добавляющий подпись нового типа в apt.

Сначала подумаю и запишу некоторые общие соображения и ожидания относительно поведения apt;
исходя из них посмотрю и прокомментирую код.

## Проверка подписи в логике работы apt

Общие соображения:

1. проверка подписи нового типа должна происходить в тех же местах кода, что и имеющаяся уже проверка подписи; с той же обязательностью успешности проверки или отказом работать.
2. обязательность успешной проверки в случае, если мы ожидаем подписи.

На счёт 2. в коде apt у меня были сомнения, надо перечитать и подумать. Но вообще я ожидаю, что если в sources.list указано, каким ключом подписано (в квадратных скобках), то проверка обязательно должна быть успешна, иначе отказ работать. Для подписи нового типа я бы ожидал такого же поведения: указание в sources.list -- обязательное требование. Как это совместить с двумя типами подписи -- надо ещё подумать. (Ну под одними именем два публичных ключа разного типа, наверное.)

## О свойствах реализаций проверки подписи через внешний инструмент

## Комментарии к коду (коммитам)

<!-- Local Variables: -->
<!-- ispell-local-dictionary: "russian" -->
<!-- End: -->
