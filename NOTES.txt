Спасибо за учёт всех замечаний!

Спасибо и за написание .editorconfig!

Работает в emacs с глобальным (editorconfig-mode) без .dir-locals.el.
Проверил в vim с .vimrc:

$ cat ~/.vimrc
colorscheme elflord
set belloff=all
set modeline
set secure
set exrc
packadd! editorconfig

Замечено расхождение в поведении, когда .editorconfig убран. Без
.editorconfig нажатие таба в vim делает отступ в 8.

Меняется set softtabstop=3

Мне это не важно. И это по сути не стиль оформления проекта, а
особенности взаимодействия конкретного человека с
редактором. Единственное только: нарушается единообразие поведения при
учёте .editorconfig и без.

* * *

С --no-compression хорошо получилось.
